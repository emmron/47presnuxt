---
const { onCtaClick = '#discussion' } = Astro.props;
---
<section class="hero" id="top">
  <div class="container hero-inner">
    <div class="hero-copy">
      <span class="tag">Perth Crypto Newsdesk</span>
      <h1>WA's daily signal on global digital assets.</h1>
      <p>
        Get the latest bitcoin, Ethereum, and altcoin developments filtered through a Perth lens.
        Follow local adoption stories, market sentiment, and upcoming meetups in one place.
      </p>
      <div class="hero-actions">
        <a class="button-primary" href={onCtaClick}>
          Join the community feed
          <span aria-hidden="true">→</span>
        </a>
        <a class="hero-link" href="#subscribe">Get email briefings</a>
      </div>
      <dl class="hero-stats">
        <div>
          <dt>Perth traders polled this week</dt>
          <dd>312</dd>
        </div>
        <div>
          <dt>Local crypto businesses tracked</dt>
          <dd>67</dd>
        </div>
        <div>
          <dt>Community watchlist tokens</dt>
          <dd>24</dd>
        </div>
      </dl>
    </div>
    <div class="hero-visual">
      <div class="glow"></div>
      <div class="panel">
        <div class="panel-header">
          <span>Market Mood</span>
          <strong class="bullish">Bullish 63%</strong>
        </div>
        <ul>
          <li>
            <span>BTC / AUD</span>
            <strong>$104,560</strong>
            <small class="up">+2.4%</small>
          </li>
          <li>
            <span>ETH / AUD</span>
            <strong>$6,290</strong>
            <small class="up">+1.7%</small>
          </li>
          <li>
            <span>ARB / AUD</span>
            <strong>$2.45</strong>
            <small class="down">-0.6%</small>
          </li>
          <li>
            <span>BTC Hash Rate</span>
            <strong>607 EH/s</strong>
            <small class="steady">steady</small>
          </li>
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 6rem 0 5rem;
    background: var(--gradient-hero);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    position: relative;
    overflow: hidden;
  }

  .hero::after {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 50% 120%, rgba(59, 245, 255, 0.18), transparent 60%);
    z-index: 0;
  }

  .hero-inner {
    position: relative;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 3.5rem;
    align-items: center;
    z-index: 1;
  }

  .hero-copy h1 {
    font-family: 'Space Grotesk', sans-serif;
    font-size: clamp(2.4rem, 5vw, 3.4rem);
    margin: 1rem 0 1.2rem;
    letter-spacing: -0.03em;
  }

  .hero-copy p {
    color: var(--color-muted);
    font-size: 1.05rem;
    max-width: 46ch;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin: 2rem 0 2.25rem;
    flex-wrap: wrap;
  }

  .hero-link {
    color: var(--color-primary);
    font-weight: 600;
  }

  .hero-stats {
    display: grid;
    gap: 1.1rem;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  }

  .hero-stats dt {
    color: rgba(245, 247, 255, 0.55);
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .hero-stats dd {
    margin: 0.35rem 0 0;
    font-size: 1.5rem;
    font-weight: 700;
  }

  .hero-visual {
    position: relative;
    display: flex;
    justify-content: center;
  }

  .glow {
    position: absolute;
    width: 320px;
    height: 320px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(59, 245, 255, 0.4), rgba(59, 245, 255, 0.05) 70%);
    filter: blur(0px);
    opacity: 0.8;
  }

  .panel {
    position: relative;
    z-index: 1;
    width: min(320px, 90%);
    background: rgba(10, 12, 25, 0.9);
    border: 1px solid rgba(59, 245, 255, 0.25);
    border-radius: 24px;
    padding: 1.5rem;
    box-shadow: var(--shadow-lg);
    backdrop-filter: blur(14px);
  }

  .panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1.4rem;
  }

  .panel-header span {
    color: var(--color-muted);
    font-size: 0.9rem;
  }

  .panel-header .bullish {
    color: var(--color-primary);
    font-weight: 700;
  }

  .panel ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1.1rem;
  }

  .panel li {
    display: grid;
    grid-template-columns: 2fr 1fr auto;
    align-items: baseline;
    column-gap: 0.75rem;
    color: rgba(245, 247, 255, 0.85);
    font-size: 0.95rem;
  }

  .panel strong {
    font-size: 1.05rem;
  }

  .panel small {
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .panel .up {
    color: #58ffb1;
  }

  .panel .down {
    color: #ff6ec7;
  }

  .panel .steady {
    color: rgba(245, 247, 255, 0.6);
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: 4.5rem;
    }

    .hero-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .panel li {
      grid-template-columns: 1.6fr 1fr 0.8fr;
    }
  }
</style>
